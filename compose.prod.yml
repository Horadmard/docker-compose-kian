services:
  nginx-proxy:
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/etc/nginx/certs:ro 
  backend:
    image: ghcr.io/kianarhnm/project1-backend:${BACKEND_VERSION}
    environment:
      DEBUG: "false"

  frontend:
    image: ghcr.io/kianarhnm/project1-frontend:${FRONTEND_VERSION}
    environment:
      NODE_ENV: production

